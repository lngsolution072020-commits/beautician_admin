rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Deny all client access by default; backend uses Admin SDK for uploads.
    // To allow signed-in users to upload, use: allow read, write: if request.auth != null;
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
